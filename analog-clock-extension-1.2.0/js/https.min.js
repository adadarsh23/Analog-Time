const toggleBtn=document.getElementById("toggleBtn");chrome.storage.sync.get("httpsWarningEnabled",({httpsWarningEnabled:httpsWarningEnabled})=>{const isEnabled=httpsWarningEnabled??true;updateButton(isEnabled)});toggleBtn.addEventListener("click",()=>{chrome.storage.sync.get("httpsWarningEnabled",({httpsWarningEnabled:httpsWarningEnabled})=>{const newState=!(httpsWarningEnabled??true);chrome.storage.sync.set({httpsWarningEnabled:newState},()=>{updateButton(newState);chrome.tabs.query({active:true,currentWindow:true},tabs=>{if(tabs[0]?.id)chrome.tabs.reload(tabs[0].id)})})})});function updateButton(isEnabled){toggleBtn.textContent=isEnabled?"Turn OFF":"Turn ON";toggleBtn.className=isEnabled?"on":"off"}
//# sourceMappingURL=js/https.js.map